<Window x:Class="luiza.redac"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:luiza"
        mc:Ignorable="d"
        Title="redac" Height="400" Width="400">
    <Grid>
        <TextBox x:Name="login" Text="{Binding logins}" IsEnabled="False" Height="30" Width="150" Margin="0,0,0,50"/>
        <TextBox x:Name="pass" Text="{Binding passwords}" IsEnabled="False" Height="30" Width="150" Margin="0,50,0,0"/>
        <ComboBox x:Name="roles" ItemsSource="{Binding Role.rol}" Text="{Binding Role.rol}" Height="30" Width="150" Margin="0,140,0,0"/>
        <Button Height="30" Width="150" Margin="0,0,0,70" VerticalAlignment="Bottom" Content="Сохранить" Click="Button_Click" />
    </Grid>
</Window>
